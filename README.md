# 🐾 Paw Map (포우맵)

RP 중 장소를 기록하고, 장소별 실시간 커뮤니티 피드를 생성하는 SillyTavern 확장입니다.

> v0.9.2부터 모든 자동 장소 감지가 `autoDetect` 마스터 스위치(기본 OFF) 뒤로 비활성화되어, 기본값은 완전 수동 입력입니다. 장소는 직접 추가/수정/삭제하고, 드래그→이벤트 기록은 설정에서 on/off 할 수 있습니다.

---

## 기능

### 장소 관리
- 장소 직접 추가/수정/삭제 (한국어/영어)
- Leaflet 기반 실제 세계지도
- 장소별 노드맵 약도
- 걸어서 거리 계산

### 실시간 커뮤니티 피드
장소마다 트위터 스타일 짧은 포스트 피드를 생성합니다.

- 포스트 4~12개 생성 (설정으로 조절)
- 장소 해시태그 기반 주민 반응
- 현지 음식·문화·언어 자동 반영
- 목격담 트윗 (최근 RP 맥락 활용)
- 답글, 이미지 자동 첨부
- 여러 톤 섞어서 생성 (담백/약한 감성/폭발 감성)

### 현지 정보 보강 (선택)
- **OFF**: LLM 학습 데이터만 사용
- **Nominatim**: 장소 주변 500m POI 정보를 프롬프트에 추가 (무료)
- **Google Search Grounding**: 실시간 웹 검색 결과 반영 (유료)

### NPC 관리
- 장소별 등장 NPC 자동 감지
- 호감도, 성격, 관계 메모 관리

### 기타
- 채팅 드래그 → 요약 후 이벤트 저장
- 모바일 대응 디버그 로그 뷰어
- 그룹챗 다인 캐릭터 대응

---

## 설정

### API 키 등록
설정 → LLM API 키에서 제공자 선택 후 키 입력:

- Google AI Studio: https://aistudio.google.com/apikey
- Vertex AI (Express mode 또는 서비스 계정 JSON)
- OpenAI / OpenRouter / Claude

### 추천 모델
- **Gemini 2.5 Flash** (기본 추천, 빠르고 저렴)
- Gemini 2.5 Pro (느리고 토큰 많이 사용, 필요시에만)
- Gemini 2.5 Flash Lite (최저 비용)

### 옵션
- **생성 분량**: 가벼움(4~5개) / 기본(7~9개) / 풍성함(10~12개)
- **현지 정보 보강**: OFF / Nominatim / Google Grounding

---

## 비용 참고

Gemini 2.5 Flash 기준 대략:

| 분량 | 1회 | 월 450회 |
|---|---|---|
| 가벼움 | ~6원 | ~2,700원 |
| 기본 | ~14원 | ~6,300원 |
| 풍성함 | ~22원 | ~9,900원 |

Google AI Studio 무료 티어는 일 1,500회까지 지원되어 대부분 유저는 무료로 사용 가능합니다.

Google Search Grounding은 1,000회당 $35 (별도 유료).

---

## 문제 해결

에러 발생 시:
1. 설정 → 🐛 마지막 LLM 응답 보기
2. 📋 복사 버튼으로 로그 복사
3. 로그와 함께 리포트

모바일에서도 F12 없이 디버그 가능합니다.

---

## 변경 이력

### v0.9.41
- 🔥 [핵심 버그] **현재 이동 감지가 v0.9.0부터 완전히 죽어있었음** — 활성 `scanMessage()`가 즉시 `return false`만 하고, 실제 로직(`_legacyScanMessage`)은 호출되지 않는 상태. v0.9.23에서 추가한 detectMode·confirm·지오코딩 등 모든 개선이 죽은 코드에 들어가 있었음. scanMessage를 실제 로직에 연결 → detectMode 설정이 비로소 동작
- [confirm 모드] 확인 팝업이 조용히 안 뜨던 문제 — `#send_but` 존재/표시 여부로 막던 가드 제거(독립 오버레이라 불필요)

### v0.9.40
- [진단] 디버그 로그(🐛)에 감지 설정 상태 추가 — `enabled` / `🔍 detectMode(장소감지)` / `📅 autoSchedule` / `📝 autoEvent` / `autoDetectPaused`. detectMode가 OFF면 현재 이동이 감지 안 되는 게 한눈에 보임. 감지가 안 될 때 이 로그를 확인

### v0.9.39
- [영어 감지 보강] 실제 RP 영어 문장으로 검증하며 영어 이동 동사 대폭 확장 (engMoveVerbs: reach·reaching·make it to·pull into·stop by·step into·head into·landed in/at 등). "managed to reach Caesar's share house" 같은 타동사+소유격 목적어도 인식
- 검증: Tokyo/JFK Airport/New York/share house/football field/arrivals hall 정상 감지, 도로(Van Wyck Expressway)·향수성 언급("peace of Cabo")은 오탐 없이 제외

### v0.9.38
- [감지 버그] 영어 "we arrived **in** Tokyo"가 감지 안 되던 문제 — 이동 키워드에 "arrived at"만 있고 "arrived in"(도시엔 보통 in 사용)이 빠져있었음. arrived in/to·landed·flew into·made it to·got to 등 영어 도착/이동 표현 대폭 보강

### v0.9.37
- [핀 교정] 이름에 도시/국가가 있는데 집 근처에 잘못 박힌 임시/예정 장소("하네다 공항 VIP 의전실, 도쿄, 일본" → 서울)를 채팅 열 때 **그 도시로 자동 재지오코딩**(도쿄로 이동). 기존에 잘못 박힌 장소도 교정됨. `_geoFixed` 플래그로 1회만 처리(중복 호출 방지)

### v0.9.36
- [로고] "PAW MAP" 대문자가 화면에 안 바뀌던 문제 — 텍스트뿐 아니라 **CSS로도 강제 대문자**(`text-transform:uppercase`) 적용. (설치 후에도 그대로면 확장 재설치/캐시 새로고침 필요)
- [핀 정확도] "도쿄 롯폰기 호텔 스위트룸"처럼 구체적인 이름이 지오코딩 실패해 집 근처에 박히던 문제 — **이름 속 도시("도쿄")를 찾아 그 위치로 지오코딩**하는 폴백 추가 (scanMessage + scanSchedule 둘 다). 도시 못 찾으면 기존처럼 현재 근처

### v0.9.35
- [감지 버그 수정] 알려진 도시/지역/국가를 **최우선 감지** — "카보 산 루카스에 도착" → 이전엔 "루카스"만 잡히던 게 이제 "카보" 정상 인식. 도시 있으면 조각 단어보다 우선
- [오탐 수정] "교차로가" → "교차" 처럼 도로/조각 단어를 장소로 잘못 잡던 것 차단 (교차로·스크램블·한복판·통창·인파 등 스킵)
- [로고] "Paw Map" → **"PAW MAP"** 대문자로 변경 (헤더·런처·탭·지도 태그 전부)

### v0.9.34
- [지오코딩 일반화] 일반 장소 감지("도쿄에 도착했다")도 **지역/도시/국가명이면 지오코딩해서 실제 위치에 핀** (예정 일정뿐 아니라 현재 이동도). 일반/가상 장소는 현재 근처 유지 — `placeOnly` 필터로 임의 POI 오매칭 방지(class place/boundary만)
- [감지 보강] 도시/국가 인식 목록 확장 — 국가(한국·일본·미국·멕시코 등 한/영) + 관광지(칸쿤·카보·하와이·발리 등) 추가 → 감지 + 정확한 핀 직결
- [🐛 버그 수정] LLM 토큰/thinking 오버라이드가 호출 에러 시 정리되지 않아 **다음 LLM 호출에 누수**(256토큰 등으로 다른 응답이 잘리던)되던 버그 — `callLLM`을 try/finally로 감싸 매 호출 후 무조건 초기화

### v0.9.33
- [핀 보장] 예정 일정 장소가 여전히 핀이 안 꽂히던 문제 — 지오코딩 실패 시 폴백이 현재 위치 GPS에만 의존했음. 이제 **현재 위치 → 좌표 있는 아무 장소 → 기본값** 순으로 무조건 좌표 부여(핀 100% 보장)
- [정확도] 일정 LLM에 현재 위치를 알려줘 `geo` 정확도↑ (예: 멕시코 카보면 "만타 레스토랑, 카보 산 루카스"). 지오코딩 1회 재시도(레이트리밋 대응)
- [기존 데이터 보정] 채팅 열 때 좌표 없는 임시/예정 장소에 핀 좌표 자동 보정 — 예전에 핀 없이 생긴 장소도 보이게

### v0.9.32
- [핀] 예정 일정 장소가 내 페이지엔 뜨는데 **지도에 핀이 안 꽂히던 문제 수정** — 렌더러는 좌표 없는 장소를 건너뛰는데 생성 시점에 좌표를 못 받던 경우가 있었음
- [타지역/국가] 일정 LLM이 `geo`(지도 검색용 실제 위치, 다른 도시·국가 지명 포함) 추출 → 새 임시 장소를 **지오코딩**: 실제 위치를 찾으면 거기(타지역/국가 포함)에 핀, 못 찾으면 현재 위치 근처에 강제 배치(핀 보장). 실패해도 경고 토스트 없음(조용한 폴백)

### v0.9.31
- [#4 재수정] 타임라인 시트 드래그 시 삭제/초기화 오발동 — 진짜 원인은 **시트 내용 스와이프**(핸들 아닌 곳). 삭제·초기화 버튼을 `touchend` 제거하고 `click`만 받게 변경(스와이프/스크롤은 click이 취소돼 발동 안 함)
- [#2] 지도 위 검색창을 거의 투명하게(불투명도 0.16 + 옅은 블러) — 뒤 지도가 비침
- [#3] 장소 감지 한국어 오탐 필터 보강 — 신체/추상 명사(얼굴·어깨·표정·마음·시선 등)가 이동 문장에서 장소로 잘못 잡히던 것 차단

### v0.9.30
- [#1] 지도 위 검색창의 중복 닫기(✕) 버튼 제거(헤더 ✕로 닫음) + 검색창 더 반투명하게(불투명도 0.6 + 블러)
- [#2/#3] 내 페이지·지도 핀·현재 위치 등 **장소를 누르면 지도도 그 위치로 이동**(flyTo) — 지도 패닝 후 현재 위치로 돌아오기 쉬워짐
- [#4] 타임라인 바텀시트를 half→full로 **드래그할 때 삭제/초기화 버튼이 오발동**하던 버그 수정 — 드래그 직후(400ms) touchend는 무시

### v0.9.29
- 🏰 판타지 모드 — 아직 미완성이라 **활성화 비활성 + "업데이트 예정 중" 안내**로 전환 (버튼은 흐리게 티저로 유지, 눌러도 토글 안 되고 안내 토스트만). 예전에 켜둔 사용자도 강제 비활성

### v0.9.28
- [UX] Paw Map 풀스크린에서 투박한 플로팅 ✕ 대신 **상단 헤더(✕ 포함)를 그대로 유지** — 맵이 헤더 높이만큼만 살짝 줄어들고, 깔끔한 닫기 버튼이 항상 노출됨 (플로팅 버튼 제거)

### v0.9.27
- [버그] v0.9.26 플로팅 닫기 ✕ 가 패널을 닫아도 화면에 남아있던 문제 수정 — 패널은 display:none이 아니라 클래스로 여닫혀서 fixed 버튼이 안 사라졌음. 닫기 시 명시적으로 숨기게 처리

### v0.9.26
- [버그/UX] 확장이 화면 꽉 차게(Paw Map 풀스크린) 떠서 닫을 수 없던 문제 수정 — 헤더가 숨는 풀스크린 모드에서도 **항상 떠있는 플로팅 닫기 버튼(✕, 우상단)** 추가. 배경 탭이 안 되는 상황에서도 새로고침 없이 닫힘

### v0.9.25
- 예정 일정 감지가 **유저 입력 + AI 출력 둘 다** 읽도록 변경 (약속은 유저가 먼저 꺼내는 경우 많음)
- 일정에서 새 장소가 나오면 **지도에 자동 핀**(현재 위치 반경 30~150m 내 임시 좌표) + **임시 장소로 표시**(이름/위치 사용자 수정 가능, memo·wantToGo 태그)

### v0.9.24
- **📅 예정 일정 자동 기록** (autoSchedule 토글) — RP에서 약속/예정(내일·예약·만나자 등)을 감지하면 LLM이 **장소 + 예상 일시 + 내용**을 추출해 기록. 장소 감지 모드와 독립적으로 동작
  - 기존 isPlan/planDate 형식으로 저장 → **장소별 "예정" 섹션 + 타임라인 예정 집계에 자동 표시**
  - 같은 내용 중복 방지, plan 신호 있을 때만 LLM 호출(비용 최소화), 기본 OFF(옵트인)

### v0.9.23
- **장소 감지 모드 추가** (설정) — 끔(수동) / **확인 후 등록(팝업)** / 자동 등록 3단 선택
  - 확인 모드: 새 장소 감지 시 팝업으로 떠서, 맥락에서 잡은 이름을 **수정**해 등록하거나 무시 가능
  - 기존 autoDetect 사용자는 자동으로 '자동 등록'으로 이관
  - (현재는 새 top-level 장소 감지에 적용. 기존 장소로의 이동·서브로케이션은 그대로 자동)
- **📝 이벤트 자동 기록 on/off** 토글 추가 — 끄면 RP 텍스트에서 이벤트 자동 추출 안 함

### v0.9.22
- [버그] 터줏대감 시트가 맨 위로 튀어 고정되던 스크롤 버그 수정 (scrollIntoView block:nearest)
- [터줏대감] 생성 시 장소 유형을 먼저 추론 → 그 환경에 맞는 존재만 등장 (실내 숙소에 바다동물 X)
- [주입] 터줏대감을 RP에 강제 등장시키지 않고, 현재 씬(실내/실외·위치·시간)에 맞을 때만 자연스럽게 등장하도록 완화 (빌라 안에 갑자기 펠리컨 X)
- [리뷰] 캐릭터 드라마 위주에서 → 장소 자체에 대한 평가(분위기·뷰·서비스·청결·소음 등)를 앵커로 (RP는 목소리·맥락 제공)

### v0.9.21
- 커뮤니티 이미지 안 뜨던 문제 수정 — (1) v0.9.18에서 잘못 넣은 `referrerpolicy=no-referrer` 제거(폴리네이션이 referrer로 앱 식별/등급하는데 이게 오히려 손해였음), (2) 로드 실패 시 15초 후 1회 자동 재시도(익명 15초당 1장 레이트리밋 대응), (3) 한 피드 이미지를 1~2개로 축소(많으면 제한에 걸려 전부 실패)
- 핀(반영) 주입 텍스트 정리 — `<img>`/`<br>`/HTML 태그·해시태그·`*액션*`·URL 제거하고 순수 문장만 주입

### v0.9.20
- 진단 강화 — 커뮤니티 buzz 주입/생략 여부를 콘솔에 로그, 핀 주입 내용 로그. 디버그 뷰어(🐛)에 "Last Injected Prompt" 섹션 추가 (실제 주입 내용 확인용)

### v0.9.19
- 커뮤니티 "반영(핀)" 동작 정리 — 현재 장소에 핀한 커뮤니티 글이 있으면, 자동 "Local buzz"(최근 4개) 주입을 생략하고 **핀한 글만** 반영. (핀이 없으면 기존처럼 분위기용 자동 주입 유지)

### v0.9.18
- 폴리네이션(Pollinations) 커뮤니티 이미지 보강 — 렌더 시점에 이미지 URL 일괄 업그레이드
  - 품질: `model=flux` + `enhance=true`(AI가 프롬프트 다듬음) 자동 적용
  - 안정성: `loading=lazy`, `referrerpolicy=no-referrer`, 로드 실패 시 이미지 자동 숨김(onerror)
  - 프롬프트 키워드 자동 URL 인코딩 (공백 등 깨짐 방지) · 기존 캐시된 게시물에도 적용

### v0.9.17
- 직접 API 키 섹션을 "🔑 직접 API 키 (고급)" 접이식으로 정리 — 연결 프로필을 쓰면 기본 접힘. 폴백용으로 기능은 유지되며, 키만 있고 프로필 없으면 자동으로 펼쳐짐

### v0.9.16
- **연결 프로필로 생성 (방법 B)** — 설정의 기존 "감지 모델"을 "🔗 생성 연결 프로필"로 전환. 선택하면 리뷰/커뮤니티/NPC/요약을 SillyTavern 연결 프로필(=RP에 쓰는 그 연결) 경유로 생성 (ConnectionManagerRequestService). API 키 별도 입력 불필요
  - 옵트인(선택할 때만) + 실패 시 기존 API 키/폴백 경로로 자동 전환 → 회귀 안전
  - 서비스 탐색: getContext → window → shared.js 동적 import 순으로 방어적 접근

### v0.9.15
- Vertex AI Express 키(AQ.~) 연결 실패 수정 — 인증을 `x-goog-api-key` 헤더 대신 **`?key=` 쿼리 방식**으로 변경 (Express 모드에서 검증된 방식). 실패 시 HTTP 사유는 '마지막 LLM 응답 보기'에 표시됨

### v0.9.14
- **설정창이 안 뜨던 버그 긴급 수정** — v0.9.13에서 'ST 키 가져오기' 핸들러를 넣을 때 '세계관 이어가기' 핸들러 시작 줄이 잘못 지워져 설정 바인딩이 깨졌던 것 복구
- **Gemini + Vertex 통합** — 프로바이더 드롭다운에서 Vertex를 빼고, Gemini 선택 시 "⚙️ Vertex AI로 호출" 체크박스로 합침. 켜면 서비스계정 JSON/리전(또는 Vertex 키)으로 라우팅. 기존 Vertex 사용자는 자동 이관(Gemini + Vertex 고급 ON)

### v0.9.13
- 🔗 **ST 연결 키 가져오기** 버튼 추가 — SillyTavern에 연결된 API 키(Gemini/OpenAI/OpenRouter)를 한 번에 LLM 키 칸으로 복사. 보안상 키가 안 읽히면 "직접 입력" 안내로 자연스럽게 폴백

### v0.9.12
- 새 장소가 엉뚱하게 뉴욕(기존 GPS 장소)에 박히던 버그 수정 — 좌표 자동 배치를 **현재 위치에 GPS가 있을 때만** 그 근처로 제한 (다른 도시면 좌표 미설정 → 직접 지오코딩)
- "현재 위치 지정" 명확화 — 바텀시트 이동 pill을 "📍 여기로 / 📍 현재 위치"로, 팝오버 버튼을 "📍 현재 위치로 지정"으로 변경

### v0.9.11
- **RP 시간 따르기 복구** (회귀 버그 수정) — 방문기록·타임라인이 실제 현재 날짜가 아니라 **RP 상태창의 날짜**를 따르도록
  - v0.9.0에서 꺼졌던 RP 날짜 추출(`_extractRpDate`) 복구 (Date:/날짜:/시간:/📅/한글 "N월 N일" 등 인식)
  - 모든 이동(`moveTo`)이 명시값 없으면 최신 메시지의 RP 날짜를 자동 사용 → 첫/최근 방문일이 RP 시간으로 기록
  - 수동·드래그 이벤트도 RP 날짜로 기록
  - 직접 수정한 방문일(rpFirstVisited 등)은 그대로 유지 (덮어쓰지 않음)

### v0.9.10
- 핀(📌 반영) 주입 수정 — 이전엔 현재 장소의 핀만 주입됐는데, 이제 **어느 장소든 핀한 리뷰/커뮤니티는 모두 반영**됨 (다른 장소 건 `@장소명` 태그로 구분). 콘솔에 `📌 pinned items injected: N` 로그 추가
- 터줏대감 아바타 이모지 개선 — 분위기/날씨 이모지(🌊 등) 대신 **그 생물 자체**(펠리컨→🐦 등)를 고르게 프롬프트 강화

### v0.9.9
- 리뷰 날짜 "NaN주 전" 버그 수정 — daysAgo/stars 값이 없거나 이상해도 안전하게 표시
  - 생성 시점 정규화(저장 데이터까지) + 렌더 단 방어(기존 캐시 리뷰도) + 프롬프트에 필수 필드 명시
  - 별점 범위 벗어난 값으로 인한 잠재적 크래시(repeat 음수)도 함께 방어

### v0.9.8
- 살짝 더 사랑스럽게 (틀 유지) — 모서리 조금 더 둥글게, 버튼에 눌리는 입체감, 인풋 포커스 앰버 글로우

### v0.9.7
- 랜덤 생성 다양성 강화
  - 터줏대감: 매번 다른 아키타입 시드(사람/동물 폭넓게) + "겹치지 말고 의외성 있게" 지시
  - 리뷰: 리뷰어 라인업·별점 분포 매번 다양하게 (1~5 골고루, 혹평 1개 이상)
  - 커뮤니티: 화자 나이·직업·동물 종류 다양하게, 같은 핸들/말투 반복 금지
- 귀여운 테마 토대: Jua 디스플레이 폰트 적용(주요 제목) — 화면별 디테일 리스킨은 진행 중

### v0.9.6
- 🎲 터줏대감 랜덤 생성 — 장소 수정창에서 버튼 하나로 그 장소에 어울리는 단골/동물을 LLM이 랜덤 생성 (사람/동물 무작위)
  - 생성된 터줏대감은 프로필(호감도·소개·성격·관계)에서 수정 가능, 목록 항목 탭하면 프로필 열림
- 터줏대감 등장 강화 — "이 장소의 터줏대감(단골/거주 동물)이니, 장면이 여기서 벌어지면 실제로 등장·인사·반응하게" 로 주입 강화 (동물은 물리적으로 나타나도록)

### v0.9.5
- 장소 수정창을 풀스크린 오버레이로 전환 (덮어쓰기 + 내부 스크롤 + 헤더에 닫기/💾 저장)
- 메모 칸 역할 분리
  - 💭 내 메모 = 항상 비공개 (AI 반영 안 됨)
  - 🎬 반영 노트 = RP 반영 모드 따라 캐릭터/연출에 전달
- 커뮤니티 = "이 장소에 대한 소문/분위기"로 주입 (장면 배경에 녹임)
- NPC = 실재 엔티티로 등장 주입 (사람/동물 분리, 동물은 장면에 물리적으로 등장)
- 🔒 끔 장소는 커뮤니티·NPC도 주입 제외 (캐릭터가 모르는 장소)

### v0.9.4
- 리뷰/커뮤니티 카드 "📌 반영" 체크 → 다음 응답 프롬프트에 주입
  - 카드별 📌 버튼 (리뷰·실시간 반응 모두), 체크 유지하는 동안 계속 반영 (수동 해제)
  - 핀한 내용은 스냅샷으로 저장돼 리뷰/반응 재생성에도 유지
  - `📌 User-pinned — reflect these in your next response` 섹션으로 주입

### v0.9.3
- 장소별 "RP 반영 모드" 추가 (편집 팝업에서 선택)
  - 🔒 끔(내 메모) / 🎬 연출만(롤플 반영, 캐릭터는 모름) / 📍 캐릭터도 앎
  - 기본값: 방문한 적 있는 장소는 자동 📍, 기록만 한 장소는 자동 🔒 (직접 덮어쓰기 가능)
  - 🎬 연출 모드는 OOC 연출 지시로 주입 — "이벤트 할 장소" 등을 캐릭터 몰래 장면에 유도
  - memo/특이사항이 모드 따라 주입 (예전엔 무조건 주입 → 비공개 계획 누수 방지), 🔒 장소는 "주변" 목록에서도 숨김
- (예정) 리뷰/커뮤니티 카드 체크 → 다음 응답 반영

### v0.9.2
- 자동 장소 감지 완전 비활성화 (삭제 아님 — `autoDetect` 마스터 스위치 뒤로 가둠, 기본 OFF)
  - 캐릭터시트 주소/지역 자동 추출이 설정과 무관하게 돌던 버그 차단 (감지 껐는데도 장소 생성되던 원인)
- ✂️ 드래그 이벤트 기록 on/off 설정 토글 추가 (기본 ON)
- ST 데이터 호출 충돌 방어
  - ST 응답 생성 중에는 확장의 자동 작업 스킵 (`GENERATION_STARTED` 추적, 45초 자동 해제)
  - 비동기 작업(LLM 요약·지오코딩·DB 쓰기) 도중 채팅이 바뀌면 결과 폐기 → 엉뚱한 채팅 오염 방지

### v0.9.0
- 드래그 요약 버튼을 원형 플로팅 버튼으로 변경 (네이티브 선택 메뉴와 겹침 해결)

### v0.8.7
- 그룹챗 다인 캐릭터 대응 (최근 발화자 자동 추출)
- Grounding 타임아웃 시 자동 폴백
- 동적 타임아웃 (Grounding 90초, 일반 60초)
- Pro 모델 maxTokens 자동 증량

### v0.8.0
- 현지 정보 보강 기능 추가 (Nominatim, Google Grounding)
- 커뮤니티 톤 밸런싱 (30/40/30)
- 한국어 맞춤법 가이드 추가

### v0.7.x
- 장소 특화 트윗 시스템
- 현지 색깔 반영 (나라별 특산물·문화)
- 목격담 트윗
- 디버그 뷰어 (모바일 대응)

### v0.6.x
- 실시간 커뮤니티 피드 도입
- 바텀시트 기반 UI
- Gemini API 폴백 체인

---

## 기술 스택

- SillyTavern (확장 호스트)
- Leaflet (지도)
- Nominatim + Overpass API (POI 검색)
- Gemini API (이벤트 추출, 커뮤니티 생성)
- Google Search Grounding (선택, 유료)
- Pollinations.ai (이미지)
